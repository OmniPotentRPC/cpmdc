PEF embed enables OpenCPMD `cntl%tpres`, snapshots Cartesian stress
(`paiu/omega` as Ha/Bohr^3, row-major 9-vector), exports via
`cpmdc_last_stress` and `PotentialResult.stress` with ForceInput unit conversion.
