Live OpenCPMD PEF multi-force now requests ionic forces via
`cpmdc_set_need_forces` (vendored OpenCPMD `tfor` patch), documents
`CPMDC_PSEUDO_DIR` cold memfd resolution, and guards the force-export path in
tests.
